/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "plus.unsplash.com",
      "images.unsplash.com",
      "picsum.photos",
      "ucarecdn.com",
    ],
  },
};

module.exports = nextConfig;
